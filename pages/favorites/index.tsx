import styles from './favorites.module.scss';

function Favorites() {
    return (
        <section className={styles['favorites']}>
            <h2>Favorites</h2>
        </section>
    )
}

export default Favorites;