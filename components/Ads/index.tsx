import styles from "./ads.module.scss";

function Ads() {
    return (
        <section className={styles.wrapper}>
            <div className={styles.ads}>
                <p>ADS</p>
            </div>
        </section>
    );
}

export default Ads;
